#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <unordered_map>
using namespace std;

string convert(string s,int numrows)
{
    int j = (2*numrows) - 2;//this will the index of the next index to be in the same row.
    string t = "";

    for(int i=0;i<numrows-1;i++)
    {
        
    }

}

int main()
{
    string s;
    cin >> s;

    int numrows;
    cin >> numrows;
}