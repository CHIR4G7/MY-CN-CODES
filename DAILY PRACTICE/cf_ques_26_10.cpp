#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <unordered_map>
using namespace std;

int len(char str[])
{
    int count=0;
    for(int i=0;str[i]!='\0';i++)
    {
        count++;
    }
}

void f1(char str[])
{
    for(int i=0;i<len(str);i++)
    {
        
    }
}
 
int main()
{
    char str[100];
    cin >> str;
}