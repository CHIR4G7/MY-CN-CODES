#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <unordered_map>
#include <string>
using namespace std;

int main()
{
    string s;
    cin >> s;

    string t = "";

    bool turn = false; // false indicates alice turn..true indicates bobs turn.

    int len = s.length();
    int k = 0;
    int i = 0;              // front index
    int j = s.length() - 1; // end index;

    while (len != 0)
    {
        int i = 0;              // front index
        int j = s.length() - 1; // end index;

        if (turn == false)
        {
            
        }
        len = s.length() - 1;
    }
    cout << t;
}