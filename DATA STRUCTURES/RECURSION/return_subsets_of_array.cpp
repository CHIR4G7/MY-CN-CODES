#include <iostream>
using namespace std;

void helper(int index,int input[],int n,int output[][20],int count)
{
    if(index>=n)
    {
        output[]
    }
}

int subsets(int input[], int n, int output[][20])
{
    int count = 0;
    helper(0,input,n,output,count);
}

int main()
{
    int n;
    cin >> n;

    int input[20];
    int output[3500][20];
    for (int i = 0; i < n; i++)
    {
        cin >> input[i];
    }
}